HOST=
PORT=
DEBUG=

# JWT configuration
JWT_SECRET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Token algorithm
ALGORITHM="HS256"

# Database credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=

# Constructed URLs
# Use these constructed URLs in your application code via settings
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Test Database (SQLite) - Use a separate file for tests
TEST_DATABASE_URL=sqlite+aiosqlite:///./test_wellora.db

REDIS_URL=

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600
